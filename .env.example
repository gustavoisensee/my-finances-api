# Database Configuration
# ======================
# Get these from your Supabase project dashboard
# Settings → Database → Connection String

# Pooled connection (for application runtime)
# Uses port 6543 with PgBouncer for connection pooling
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection (required for migrations)
# Uses port 5432 for direct database access
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# Server Configuration
# ====================
# Port where the API server will run
NODE_PORT=3001

# Authentication
# ==============
# Secret key for JWT token signing
# IMPORTANT: Generate a strong random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_TOKEN=your-super-secret-jwt-token-change-this-in-production

# Admin User Configuration
# ========================
# These credentials are used to create the initial admin user when seeding the database
# Run: pnpm db:seed
ADMIN_EMAIL=admin@myfinances.com
ADMIN_PASSWORD=change-this-secure-password
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=User

# Instructions:
# 1. Copy this file to .env
# 2. Replace [PROJECT-REF], [PASSWORD], and [REGION] with your Supabase credentials
# 3. Generate a secure JWT_TOKEN for production environments
# 4. Set a strong ADMIN_PASSWORD for the initial admin user
# 5. Never commit the .env file to version control

